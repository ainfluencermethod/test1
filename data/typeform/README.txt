Typeform local store

Use submissions.jsonl for append only storage.
Use submissions.json for a JSON array if needed.
The dashboard reads submissions.jsonl first, then submissions.json.

Webhook target:
/api/typeform/webhook
